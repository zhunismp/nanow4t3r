name: "Production CI"

on:
  push:
    paths-ignore: [ '**/services/**/version.txt' ] # Ignore version.txt changes
    branches:
      - master

permissions:
  contents: write
  pull-requests: write

jobs:
  product-service-workflows:
    uses: ./.github/workflows/product-workflows.yaml
    name: Product service
    secrets: inherit
